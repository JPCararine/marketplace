import { View } from "react-native";
import { useReviewBottomSheetViewModel } from "./useReviewBottomSheet.viewModel";

export default function ReviewBottomSheetView({}: ReturnType<typeof useReviewBottomSheetViewModel>) {
    return (
        <View></View>
    )
}